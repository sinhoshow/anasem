structs{
    struct pessoa{
        string nome;
        int idade;
    };

    struct empresa{
        string nome;
        int valor;
    };

    struct funcionario extends pessoa{
        real valor;
    };

}

typedefs{
    typedef struct funcionario func;
    typedef struct pessoa p;
}

const{
    real base_calculo = 0.2;
    real a = 3;
    real a = 4;
}

var{
    int n, b, a, cidade;
    string codigo[];
    boolean b;
    int mille;
    string cidade;
}

functions{
    function real calcula_extra(real salario){
        var{
            int a[][], b, f;
            real pessoa, felipe;
        }

        local.pessoa = calcula_extra2(local.pessoa, global.base_calculo, local.a[2][1]);
       
        return global.base_calculo;
    }
    function real calcula_extra2(real salario, real codigo, int salario){
        var{
            real a;
        }
        return local.codigo;
    }
}

procedures{
    procedure calcula_extra(string cidade, int cep, string cidade){
        var{
            string estado, alfa;
            int cep, alfa;
        }
    }
}

start(){
    var{
        int numero;
        p pessoa;
        boolean codigo;
        real r;
    }
    calcula_extra(local.r);
    local.numero++;
}