structs{
    struct pessoa{
        string nome;
        int idade;
    };

    struct empresa{
        string nome;
        int valor;
    };

    struct funcionario extends pessoa{
        real valor;
    };

}

typedefs{
    typedef struct funcionario func;
    typedef struct pessoa p;
}

const{
    real base_calculo = 0.2;
    real a = 3;
    real a = 4;
}

var{
    int n, b, a, cidade;
    string codigo[];
    boolean b;
    int mille;
    string cidade;
}

functions{
    function real calcula_extra(real salario){
        var{
            int a[][], b, f;
            pessoa p, felipe;
            string teste;
            empresa e;
        }

        local.p.idade = calcula_extra2(local.e.nome, 12.5, 32);
        
        return global.base_calculo;
    }
    function int calcula_extra2(int salario, real codigo, int salario){
        var{
            real a;
        }
        return local.salario;
    }
}

procedures{
    procedure calcula_extra4(string cidade, real cep, string cidade){
        var{
            string estado, alfa;
            int cep, alfa;
        }
    }
}

start(){
    var{
        int numero;
        p pessoa;
        boolean codigo;
        real r;
    }
    calcula_extra(local.r);
    local.numero++;
}